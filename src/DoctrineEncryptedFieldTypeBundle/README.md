# DoctrineEncryptedFieldTypeBundle

## Usage

Use one of these field types for automatic encryption of it's value:

- encrypted_data_string
- encrypted_data_text
- encrypted_data_datetime

## Notes

These field types do not work with embeddables.
